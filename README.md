# backend


## Backend Installation

1. Create a virtual environment:
   ```bash
   python3 -m venv .venv

for windows   
source .venv/bin/activate

for macos
.venv\Scripts\activate


pip install -r requirements.txt

python app.py


This provides all the necessary steps to install dependencies and run your Python backend project.
